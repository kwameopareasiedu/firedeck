import { FiredeckUserConfig } from "shared/firedeck-config";

/** Type-helper to easily define a Firedeck config object */
export function defineConfig(config: FiredeckUserConfig) {
  return config;
}

export { FiredeckUserConfig };
